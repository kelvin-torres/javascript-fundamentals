let scriptSeparation = 'CHALLANGES';
console.log(scriptSeparation);

//CHALLANGE 1
/*
const markMass = 78;
const markHeight = 1.69;
const markBMI = markMass / (markHeight * markHeight);
console.log(markBMI)

const johnMass = 92;
const johnHeight = 1.95;
const johnBMI = johnMass / (johnHeight * johnHeight);
console.log(johnBMI)

const markHigherBMI = markBMI > johnBMI

console.log(markHigherBMI)

//CHALLANGE 2

if (markBMI > johnBMI) {
    console.log(`Mark's BMI ${markBMI} is higher than John's BMI ${johnBMI}`)
} else {
    console.log(`John's BMI ${johnBMI} is higher than Mark's BMI ${markBMI}`)
}

//CHALLANGE 3

const dolphinsAverage = (97 + 112 + 101) / 3;
const koalasAverage = (109 + 95 + 106) / 3;

console.log(dolphinsAverage, koalasAverage);

if (dolphinsAverage >= 100 && dolphinsAverage > koalasAverage) {
    console.log(`Dolphins 🐬 won the trophy 🏆 with the average score of ${dolphinsAverage}`)
} else if (koalasAverage >= 100 && koalasAverage > dolphinsAverage) {
    console.log(`Koalas 🐨 won the trophy 🏆 with the average score of ${koalasAverage}`)
} else if (dolphinsAverage === koalasAverage && dolphinsAverage, koalasAverage >= 100) {
    console.log(`Draw! 😓`)
} else {
    console.log(`No team wins the trophy 😞`)
};
*/

//CHALLANGE 4

const bill = 275;
const tip = bill >= 50 && bill <= 300 ? bill * 0.15 : bill * 0.2;

console.log(`The bill was $${bill}, the tip was $${tip}, and the total value is $${bill + tip}`)

